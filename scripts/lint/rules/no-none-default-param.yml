id: no-none-default-param
language: python
rule:
  pattern: "$PARAM = None"
  inside:
    kind: parameters
message: "None default parameter — use empty structures or sentinel objects"
severity: warning
